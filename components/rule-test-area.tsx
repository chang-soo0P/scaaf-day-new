"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function RuleTestArea() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Rule Test Area</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Rule testing coming soon...</p>
      </CardContent>
    </Card>
  );
}

