"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function RuleTemplates() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Rule Templates</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Rule templates coming soon...</p>
      </CardContent>
    </Card>
  );
}

